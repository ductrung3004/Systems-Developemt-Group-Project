
# MySQL host (use localhost if running locally)
DB_HOST=localhost

# MySQL username
DB_USER=root

# MySQL password
DB_PASSWORD=your_mysql_password_here

# MySQL database name (must match the imported SQL dump)
DB_NAME=paragon_db

# Default location ID assigned to new staff accounts (1 = first branch)
DEFAULT_STAFF_LOCATION_ID=1
